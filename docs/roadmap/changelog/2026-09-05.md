# 2026-09-05 — Phase 4 설계 (첫 Fable 5.1 구간)

**커밋**: 1건 · **모델**: Fable 5.1 (사용자가 `/model` 로 전환 후 착수)

## 한 일

**4-1 SPA 이식 설계서** (`docs/design/05_SPA_이식_설계서.md`, 459줄)
- db26ai 프론트 8,338줄을 탭별 줄수·상태 125개·패턴 12종으로 해부. investhub 에 없는 패턴이 절반(★)
- 결정 10건 — 핵심은 **D1 사이드바 폐지 → investhub 골격**. 사이드바 항목 전부를 ①서브탭 ②컴포저 툴바
  ③설정 서브탭 ④헤더 상태칩 ⑤보조 버튼 다섯 갈래로 매핑해 하나도 버리지 않음
- **D3 공존/롤백**: `/`=SPA, `/legacy`=기존, dist 삭제가 곧 롤백. 레거시에 hash 딥링크 3줄만 추가
- **D5 정본 하나**: 기능 레지스트리는 Python 유지, SPA·⌘K 는 API 로 읽음 (계획서의 TS 승격안 철회)
- SSE 는 investhub 에 없는 db26ai 고유 자산 — `useSse` 컴포저블로 승격, `warning` 필드 필수 표시
- 탭별 서브탭·컴포넌트 트리·라우터 분리·완료 판정을 5-0~5-7 전부 명시

**4-2 디자인 시스템** (`docs/design/06_디자인_시스템.md`, 300줄 + 캡처 6장)
- investhub 를 debug 로그인으로 실제 띄워 **헤드리스 Chrome 으로 1440px 캡처 5장** + db26ai 현재 1장
- `getComputedStyle` 측정: 헤더 56/px20, 본문 24·20/1400, h1 24/600, 카드 pad24 r8, Stat 24/600,
  서브탭 pill 8×16 14/500, 딥탭 6×12, 표 8px/14
- 팔레트: investhub 값을 그대로 잇고 **액센트만 Oracle Red(#C74634/#A83A2C/#FBEDEA)**, 헤더 #312D2A,
  SQL 블록 전용 `--code-*` 토큰(테마 무관 다크). **빨강 두 개(액센트 vs 오류)의 역할 규칙** 명시
- 컴포넌트 15종 스펙(신규 ★ 9종: SqlBlock·ResultTable·CompareView·ChatThread/Composer·PipelineProgress·
  SessionTabs·ChunkCard·ScoreGauge·StatusChips), 상태 표현(LoadingBlock 4초/20초 규칙), 편차 5분류

## 배운 것
- macOS 에 `timeout` 이 없다 → 헤드리스 Chrome 은 백그라운드 실행 + 파일 대기 + kill 로
- Chrome 헤드리스는 파일을 쓴 뒤 프로세스가 안 끝난다 — 반드시 kill
- investhub 의 세션 쿠키는 14일 영속이라 헤드리스 프로필에 남는다(debug login 1회면 됨)

## Phase 5-0 디자인 토대 (★ Fable, 같은 날)

- `web/` 62개 파일 신설. investhub 의 `package.json`·설정·`ui/` 13종·`layout/`·`main.ts`(stale-chunk)·
  `check-undef-composables.mjs` 이식, 토큰은 06 문서 팔레트로 새로 작성(레거시 별칭 블록 없이)
- db26ai 고유 ★ 7종: SqlBlock(단일 패스 토크나이저)·ResultTable(Rows 만 받음)·CompareView(동일/상이 배너)·
  EmptyState·SubTabs·Segmented·PageHeader
- **빌드가 첫 시도에 통과했다** (undef-check ✔ · vue-tsc ✔ · vite 994ms)
- FastAPI 공존 서빙: `/`=SPA, `/legacy`, `/assets`, catch-all(dist 없으면 레거시 폴백), 미정의 `/api/*` 는 JSON 404
- 레거시 `app.js` 에 해시 shim 3줄, 헤더에 "새 화면 →", v=76
- `scripts/deploy.sh`, 서빙 테스트 4개 → **pytest 50 passed**
- 검증: `/` 가 `/styleguide` 로 리다이렉트, 헤더 상태칩이 `/api/health` 실시간 반영, 콘솔 오류 0,
  `/legacy#vector` 가 Vector 탭을 연다

### 배운 것
- 숨겨진 브라우저 패널에서는 CSS `transition` 이 멈춰 `getComputedStyle` 이 중간색을 준다 → 헤드리스 캡처로 판정
- `cd` 가 다음 도구 호출까지 남는다 → 절대경로

## 다음
**5-1 Property Graph (★ Fable)** — 첫 탭. 조립 규칙의 기준을 세운다. 사용자 확인 포인트 ②(SQL 다크 블록)가 여기.
