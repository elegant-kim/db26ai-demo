# 2026-09-04 — 5개월 만의 재개

**커밋**: `8a5582b` → `2b707e5` (11건) · **모델**: Opus 5 전 구간

## 배경

2026-04-19 이후 멈춰 있던 프로젝트를 재개. 개발자가 `docs/41`·`42` SQL 이 무엇이었는지
기억하지 못하는 상태에서 시작해, **"문서만 보고 상태를 파악할 수 있는 구조"** 를
만드는 것을 최우선 목표로 계획서(`docs/ROADMAP.md`)를 세웠다.

## Phase 0 — 보안·위생 (커밋 `019d2a1`)

- `docs/` 아래 SQL 5종에 **OCI API RSA 개인키·테넌시 OCID·Groq/Google 키**가 평문.
  `.gitignore`가 그 경로를 막지 않았고 **저장소는 GitHub 공개**였다.
  → 자리표시자 치환 + `sql/setup/` 이동 + `_private/` 격리. git 이력 유출 0건 확인.
- 허용목록 37→29건 정리(사경로·평문 키 제거), worktree 정리(investhub 와 동일하게 미사용).

## Phase 1 — 재가동 점검 (커밋 `c3526d6` ~ `6c864fd`, `2b707e5`)

**이관 상태 규명**: 2026-04-14 08:50 Data Pump 이관 **완료**(SH 테이블 `LAST_ANALYZED`가
증거). 계획서 v0.1 의 "ONNX 재로드 미완" 판단은 **오판**이었고 `/api/health` 버그였다.

**고친 것**

| 건 | 내용 |
|---|---|
| `/api/health` ONNX 거짓보고 | list 를 dict 로 취급 + `except: pass`. 5개월간 `[]` 보고 |
| Property Graph (4월 미커밋분) | PGQ 집계 `ORA-49011` 0행 · SQL/PGQ 가 서로 다른 10행 → 완전일치, 14초→175ms |
| Oracle Text 인덱스 부재 | `WORLD_LEXER`+`SYNC ON COMMIT` 신설. CONTAINS 104ms vs LIKE 9,880ms |
| `hybrid_search` 가 CONTAINS 미사용 | LEFT JOIN 서브쿼리로 복구 (문서-코드 괴리) |
| `VECTOR_EMBEDDING` 행별 재평가 | 스칼라 서브쿼리로 감싸 **100배** (11.3초→0.11초) |
| ONNX 콜드스타트 | 커넥션 풀 워밍 신설. vector 5,350ms→95ms |
| 업로드가 임베딩 실패를 "성공"으로 보고 | `ORA-51932`(HNSW 차원). 실제 성공분만 집계 + 오류 노출 |
| **자연어 질문에서 하이브리드 퇴화** | `ORA-29902` → ACCUM 변환. keyword 0 → 45/41/21 |
| bare `except: pass` 5곳 · 죽은 코드 517줄 | 로깅 추가 · 삭제 |

**관통하는 패턴**: 고친 버그 다수가 **"실패를 삼키고 성공이라 보고"** 하는 코드였다.
마지막 건(자연어 하이브리드 퇴화)은 **API 검증으로는 안 잡히고 화면에서 써보고서야** 나왔다.

## Phase 2 — 문서 골격 (커밋 `f2ec83d`, `28ff138`)

- `CLAUDE.md` 3탭 → **6탭** 전면 갱신 + Critical Notes 에 실측 함정 반영
- `docs/개발노하우.md` 신설 (investhub 이식 + 이 저장소 고유 함정)
- `docs/SESSION_HANDOFF.md` 신설 — **현재 환경 실측 스냅샷**
- `scripts/check-secrets.sh` — 인라인 게이트가 셸 체이닝 탓에 **실제로는 막지 못하던** 문제 수정

## 다음

Phase 2 나머지(`docs/design/` 4종) → Phase 3 인앱 매뉴얼 → Phase 4 설계(Fable) → Phase 5 SPA 이식
